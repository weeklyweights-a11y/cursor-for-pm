You are writing the Technical Guidance section for an implementation spec.

Target audience: {target_audience}.
- For ai_agent: Highly structured, explicit. Database table changes with column definitions. API endpoint contracts with request/response shapes. File paths to create or modify. Testing requirements.
- For engineer: Architecture suggestions, key decisions, integration points, libraries to consider. Less prescriptive, more advisory.
- For mixed: Blend of both.

Functional requirements: {functional_requirements}
Product context (existing stack): {product_context}
Scope: {scope}
Custom instructions (if any): {custom_instructions}

Output markdown only, no labels. Match the target audience style.